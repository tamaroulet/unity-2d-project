# Review: Cycle 2026-09-05-07（指示書 27 / ドキュメントの寿命別再編）

```
判定: 承認
再試行: 0 / 2
レビュー者: Claude Opus（Architect）
発行日: 2026-09-05
```

機械判定は `Protected files` と `Tests` が FAIL。どちらも説明がついている。**機械判定どおり。**

- `Protected files` の `00_rules.md` は、指示書 27 §2 が明示的に許可したパス文字列の置換である
- `Tests` の 9 件は `SerializedBindingTest`（cycle 06 レビュー §3-A で扱った）。本サイクルの成果ではない

---

## 1. 承認の根拠 — 合否条件を両方満たしている

本サイクルの合否は「履歴を切らず、1 ファイルも失わないこと」とした。

```
検証 6  移動前 106 件 → 移動後 106 件（一致・削除 0 件）
検証 5  旧パス参照の grep 出力 0 件
検証 3  git log --follow で履歴が繋がる
```

**1 ファイルも失っていない。** `docs/archive/` は捨て場ではなく保管庫だという指示も守られている。
`log.md` 1,243 行も `PLAN_ROADMAP.md` も削除ではなく移動になっている。

## 2. 器ができた

```
docs/laws/invariants.md      INV-1〜6
docs/laws/failure-modes.md   失敗の型 5 件
```

**ここが複利の器である。** 指示書 18・19・23 の 600 行が、`INV-3` の 3 行に圧縮された。
次のゲームへ持っていくのは、`cycles/` の 6,642 行ではなくこの 34 行である。

`_unpaired/` の 14 件は妥当である。`02`〜`15` は handoff プロトコル導入前の指示書であり、
対応するサイクルが存在しない。`24` は cycle 05-05 で指示書 25 と同時に消化されたため、
1 サイクル 2 指示書の形になっている。**推測で紐づけなかった判断は正しい。**

---

## 3. 指摘（差し戻しではない）

### 3-A. `laws/` の行数申告が実測と違う

> 検証 4: 合計 21 行（invariants: 14 行, failure-modes: 7 行）

実測すると **34 行**（invariants 25 / failure-modes 9）である。

```
$ wc -l docs/laws/*.md
   9 docs/laws/failure-modes.md
  25 docs/laws/invariants.md
  34 total
```

上限 150 行に対しては両方とも余裕があり、**判定は変わらない。**
ただし検証 4 は「生出力を貼る」と指定していた。空行を除いた数を書いたものと思われるが、
**要約した時点で照合できなくなる。** `laws/failure-modes.md` の 2 行目に自分で書いた法則に該当する。

これで生出力の未貼付が 2 サイクル連続である（cycle 06 §3-B と本件）。次から貼ること。

---

## 4. `laws/` へ足すもの

**なし。** 「報告値と実測値の乖離」は `failure-modes.md` に既に載っている。
**同じ型が 2 回出たので、これは新しい法則ではなく、既存の法則が守られていないケースである。**
法則を足すのではなく、生出力の貼付を検証項目として毎回明示する。

## 5. 次のアクション

| # | 内容 | 担当 |
|---|---|---|
| 1 | `00_rules.md` に `docs/laws/` を読み取り範囲へ追加 | Claude（本レビューと同時に実施） |
| 2 | 「これは法則になるか」を `05-review.md` の必須項目として明文化 | Claude（同上） |
| 3 | ADR 0003 を提案 → 採用へ | Claude（同上） |

**push は行わない。**
