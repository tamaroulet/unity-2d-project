# Review: Cycle 2026-09-05-06（指示書 26 / 不変条件アサーション）

```
判定: 承認
再試行: 0 / 2
レビュー者: Claude Opus（Architect）
発行日: 2026-09-05
```

機械判定は `Tests` のみ FAIL。内訳が示されており、**機械判定どおり**として扱う。

---

## 1. 承認の根拠 — 意図的破壊が本物である

本サイクルの合否は「緑になること」ではなく「壊したときに鳴ること」とした。両方とも本物だった。

```
[INV-1] Phase 'WaitingInput' did not advance for 0.02s (timeout: 0.00s). Last action: ClickCommand_TrainButton
[INV-4] 'Canvas/CommandPanel/StudyButton/Text' missing glyphs: '勉'(U+52C9), '強'(U+5F37), '（'(U+FF08) ...
```

コードポイントを検算した。**勉 = U+52C9、強 = U+5F37、（ = U+FF08。すべて正しい。**
実際に走らせなければ出ない値である。

INV-1 は「どのフェーズで、直前に何をした後に止まったか」まで出ている。
過去 3 件（エンディング停止 / 敗北フリーズ / ショップ未結線）が、この 1 行で切り分けできる形になった。

## 2. INV-6 の扱いは正しい

```
イベント: 0 回 / ボス戦: 4 回 / ドラフト: 3 回
```

**赤のまま維持し、原因調査もしていない。** 指示書どおりである。
そしてこの 3 つの数字が、指示書 23 の目視結果（ターン 12 で発火せず）を数値で裏づけた。
**イベントは実装以来 1 度も発火していないことが、これで確定した。**

原因調査は次サイクルで扱う。

---

## 3. 指摘（差し戻しではない）

### 3-A. `SerializedBindingTest` の 9 件 FAIL に、テスト自身の欠陥が混ざっている

報告の内訳に「NUnit 戻り値型制限 8 件」とある。**これは未バインドの検出ではなく、テストの書き方の誤りである。**

このファイルは `1bdcbc9` で `auto/wip` の stash から回収したもので、
コンパイルは通ったが**一度も実行していなかった。** 今回初めて走って露見した。

指示書 12 の項目 98 は現在も `- [ ]` のままである。次サイクルで**直すか捨てるかを決める。**
本サイクルの成果ではないため、差し戻さない。

### 3-B. 報告の内訳が算術に合っていない

> 未バインド 8 件 + NUnit 戻り値型制限 8 件 → 9 件

8 + 8 は 9 にならない。**テストランナーの生出力を貼れば起きない種類の齟齬である。**
`laws/failure-modes.md` の「実測値は生出力のみ。要約で代替しない」に該当する。次から生出力を貼ること。

---

## 4. `laws/` へ足すもの

**なし。** INV-1〜6 は指示書 27 で `laws/invariants.md` に収まった。
本サイクルから新たに得られた法則は無い。

## 5. 次のアクション

| # | 内容 | 担当 |
|---|---|---|
| 1 | `SerializedBindingTest` を直すか捨てるか決める（指示書 12 項目 98） | 次サイクルの指示書 |
| 2 | `EventResolverSO` の発火条件の調査（INV-6 が赤い原因） | 次サイクルの指示書 |

**push は行わない。**
