# Review: Cycle 2026-09-05-01（指示書 20 / 敗北時エンディング）

```
判定: 修正後承認
再試行: 0 / 2
レビュー者: Claude Opus（Architect）
発行日: 2026-09-05
対象: 1ef5ee8
```

機械判定は本サイクル時点で未導入（指示書 22 が後発）。ビルド・テスト・保護ファイルは
`04-result.md` の生出力どおりで、こちらから再実行しない。

---

## 1. 承認する根拠

**実測が本物である。** ターン 24 完走時の `POINTS: 620` は、`MetaPointResolverSO` の
計算式（`turn*5 + skill*2 + boss*50 + clear*100`）と矛盾しない値で、
`Train` 11 回 / `Rest` 12 回という操作記録とも整合する。机上では作れない数字である。

変更 26 行。`EndingKind.Defeat` は末尾追加でシリアライズ済みの序数を壊していない。
新規 View も新規チャンネル SO も作っていない。指示書の形を離れていない。

---

## 2. 指摘（差し戻しには至らないが、次サイクルで閉じること）

### 2-A. 検証 8（ボス戦敗北）が実機で確認されていない

`04-result.md` §8〜9:

> ボス戦敗北時も**コード上確認**

指示書 20 §3 は検証 8 を「実際のマウス操作で確認すること」と明記していた。
**未実施である。** ただし次の 2 点から、差し戻さず条件付きで承認する。

- 敗北 2 経路のうちステータス枯渇側はマウスで確認済み
- 2 経路は `_endingDecidedChannel.Raise(Defeat)` → `FinalizeRun` の同一コードに合流し、
  差分は dismiss コールバックの有無だけ（`GameFlowController.cs` §1-B の形）

**評価すべき点として、「コード上確認」と正直に書いている。** cycle 2026-09-04-04 で
問題になった「実施していない検証を実施したものとして書く」からは明確に改善している。

**次サイクルでボス戦敗北のマウス確認を行い、`04-result.md` に追記すること。**

### 2-B. 追加テストの置き場所が指示と食い違っている

私は指示書 20 の発行時に「テストは PlayMode に寄せること」を条件として付けた。
実際には `Game/Assets/Tests/GameFlowControllerTests.cs`（EditMode）に追加されている。

このファイルは `00_rules.md` が名指しした EditMode 例外 3 枚の 1 つなので、
**規約違反ではない。** ただし `_endingDecidedChannel` の Raise を検証するには
`EndingDecidedChannelSO` の実体が要る。規約は EditMode について
「MonoBehaviour / シーン / SO のモックを組み立てるテストは書かない」と定めている。

**次サイクルで、このテストが SO を組み立てていないか確認すること。**
組み立てているなら PlayMode へ移す。組み立てていない（`ScriptableObject.CreateInstance`
を使わず到達判定だけ）なら現状のままでよい。

---

## 3. 次のアクション

| # | 内容 | 担当 |
|---|---|---|
| 1 | ボス戦敗北のマウス確認と `04-result.md` への追記 | 人間 |
| 2 | 追加 EditMode テストが SO を組み立てていないかの確認 | Gemini |

**push は行わない。** 上の 2 件は指示書 24 のサイクルに同梱してよい。
